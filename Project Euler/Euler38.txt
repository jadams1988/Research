{
	largest=0;
	for(i=1,9876,
		Num=0;
		Pan=vector(0);
		pandigital=1;
		int=1;
		while(length(Pan)<9,
			Pan=concat(Pan,NumToVec(int*i));
			int++
		);
		if(length(Pan)>9||length(Pan)<9,
			Num=0,
			Num=0;
			for(j=1,9,
				Num*=10;
				Num+=Pan[j]
			)
		);
		pandigital=IsPan9(Num);
		if(pandigital,
			largest=max(largest,Num)
		)
	);
	print(largest)
}