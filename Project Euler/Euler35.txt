{
	default(primelimit,10^6);
	TOT=0;
	forprime(i=1,1000000,
		temp=i;
		len=length(Str(i));
		NUM=vector(len);
		for(j=1,len,
			NUM[len-j+1]=temp%10;
			temp-=temp%10;
			temp/=10;
		);
		CyclicPrime=1;
		for(j=1,len,
			NewPrime=0;
			for(k=0,len-1,
				NewPrime*=10;
				NewPrime+=(NUM[(j+k-1)%len+1])
			);
			if(!isprime(NewPrime),
				CyclicPrime=0
			)
		);
		TOT+=CyclicPrime
	);
	print(TOT)
}