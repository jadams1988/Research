{
	MaxP=0;
	NumSolp=0;
	for(p=1,999,
		IntSol=0;
		for(i=1,p/(sqrt(2)+2),
			j=(2*p*i-p^2)/(2*i-2*p);
			k=sqrt(i^2+j^2);
			if(type(j)=="t_INT",
				IntSol++
			)
		);
		if(IntSol>NumSolp,
			MaxP=p;
			NumSolp=IntSol
		)
	);
	print(MaxP)
}