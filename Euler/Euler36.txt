ModTwo(a)={
	OriginalNum=a;
	Base2=0;
	int=0;
	while(OriginalNum!=0,
		Base2+=(OriginalNum%2*10^int);
		int++;
		OriginalNum-=(OriginalNum%2);
		OriginalNum/=2
	);
	return(Base2)
}
IsPal(a)={
	pal1=a;
	pal2=0;
	len=length(Str(pal1));
	while(pal1>0,
		pal2*=10;
		pal2+=pal1%10;
		pal1-=pal1%10;
		pal1/=10
	);
	if(pal2==a,
		return(1),
		return(0)
	)
}

{
	SUM=0;
	for(i=1,1000000,
		if(IsPal(i)&&IsPal(ModTwo(i)),
			SUM+=i
		)
	);
	print(SUM)
}